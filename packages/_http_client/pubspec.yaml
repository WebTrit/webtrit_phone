name: _http_client
description: Internal package to create platform-appropriate HTTP clients.
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.1.0

dev_dependencies:
  lints: ^2.1.1

name: app_transcription
resolution: workspace
description: Internal audio transcription data package of WebTrit Phone application.
version: 1.0.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  logging: ^1.3.0
  path_provider: ^2.1.5
  whisper_ggml: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0

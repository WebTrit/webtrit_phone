name: webtrit_appearance_theme_builder
resolution: workspace
description: Build-time generator of the enum-property table the JSON Schema of webtrit_appearance_theme needs.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  analyzer: ^10.2.0
  build: ^4.0.4
  dart_style: ^3.1.7
  glob: ^2.1.3

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0

include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_single_quotes: true

analyzer:
  exclude:
    - "**/*.g.dart"
    - "build/**"
    - "packages/webtrit_signaling_service/webtrit_signaling_service_android/pigeons/**"
    
formatter:
  page_width: 120

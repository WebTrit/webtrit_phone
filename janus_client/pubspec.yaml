name: janus_client
version: 0.0.1
description: Janus WebRTC Server websocket clinet.
homepage:

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  logging: ^1.0.0

dev_dependencies:
  test: ^1.16.8
  mockito: ^5.0.2
